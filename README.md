# hutch-booking
## Frontend
1. Go to the frontend directory.
`cd frontend`
2. Install dependencies.  
`npm i`
3. Run app on port 5173.  
`npm run dev`

## Backend
1. Go to backend directory (from root).  
`cd backend`
2. Install dependencies.
`npm i`
3. Run server.
`npm run start`
